🎯 **What:** Added comprehensive unit tests for `applyRoomMemoryRelationPenalty` function in `src/systems/factions.ts`.
📊 **Coverage:** Covered all code paths: handles `undefined` or `Faction.PLAYER` with no state modification, covers severity less than 5 (returns `-1` and applies mutual penalty), and covers severity greater than or equal to 5 (returns `-2` and applies mutual penalty). Tests also ensure determinism using `initFactionRelations()`.
✨ **Result:** Test coverage for `src/systems/factions.ts` is improved, safeguarding gameplay logic against unintended regressions.
