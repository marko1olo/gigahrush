🎯 **What:** The testing gap addressed
Added comprehensive unit tests covering all functions and classes inside `src/core/rand.ts` (e.g. `xorshift32`, `SeedRng`, `irand`, `randSeed`, `hashSeed`, `secureRandom`).

📊 **Coverage:** What scenarios are now tested
- Determinism and correctness of the core xorshift32 PRNG and SeedRng class logic.
- Methods for bounded outputs (e.g., bounds for `irand`, `irandFrom`).
- Deterministic shuffling (`shuffleWith`) and picking (`pickFrom`).
- Correct behaviour of global utilities bridging `Math.random` (`randSeed`, `irand`).
- Cryptographically secure value generation scaling (`secureRandom`).
- Correct collision resistant string hashing (`hashSeed`).

✨ **Result:** The improvement in test coverage
`src/core/rand.ts` is now fully covered by tests locally passing against edge conditions, ensuring procedural logic built on top of this remains sound and testable.
