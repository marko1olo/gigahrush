# Monster_30_ZOMBIE_Audit

Model: GPT-5.5  
Reasoning: xhigh  
Parallel role: existing ZOMBIE former-neighbor threat owner.

<AGENT_PROMPT id="MONSTER_30_ZOMBIE_AUDIT">
PROMPT IDENTIFIED: MONSTER_30_ZOMBIE_AUDIT | DOMAIN: Existing Monster Audit | ITERATION: monsters.md wave 1.

## Mandatory Preflight

1. Extract this XML block by id.
2. Read `README.md`, `architecture.md`, `desdoc.md`, `monsters.md`, `gatbage/monsters.md`, and `AGENTS.md`.
3. Read source: `src/entities/zombie.ts`, `src/entities/monster.ts`, `src/data/monster_ecology.ts`, `src/data/monster_variants.ts`, `src/gen/living/hospital_quarantine.ts`, `src/systems/ai/monster.ts`.
4. Create `Docs/Tasks/Status_MONSTER_30_ZOMBIE_AUDIT.md`.
5. Append final report to `Docs/AgentLogs/LOG_MONSTER_30_ZOMBIE_AUDIT.md`.
6. Run baseline `npm run typecheck`.

## Goal

Audit and polish `ZOMBIE` / **Мертвяк** as former-neighbor pressure: dangerous in crowds, kitchens, hospitals and social aftermath, but ordinary alone.

## Absolute Write Scope

Owned:
- `src/entities/zombie.ts`
- `Docs/Tasks/Status_MONSTER_30_ZOMBIE_AUDIT.md`
- `Docs/AgentLogs/LOG_MONSTER_30_ZOMBIE_AUDIT.md`
- Optional test: `tests/monster_30_zombie_audit.test.ts`

Shared files are read-only unless reassigned.

## Audit Contract

- Preserve role: human residue, isolate-from-crowd threat.
- Do not turn it into fast swarm or boss.
- Counterplay should say: pull to empty passage, avoid crowd pile, finish before contact.

## Implementation Tasks

1. Review local DEF and sprite readability as a former resident.
2. Add or sharpen `counterplay`, `floors`, `lootHint`.
3. Note any desired hospital/quarantine encounter improvements in status/log only if outside scope.
4. Run `npm run typecheck`.

## Done Means

- Мертвяк is legible as a social/aftermath enemy.
- Its threat is context, not raw stats.
</AGENT_PROMPT>
