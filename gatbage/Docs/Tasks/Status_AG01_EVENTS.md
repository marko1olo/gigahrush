# Status_AG01_EVENTS

Agent: AGENT_01_EVENTS_WORLDLOG  
Domain: Event Bus / World Log  
Task count: 12  
Prompt source: `Docs/AgentPrompts/AGENT_01_EVENTS_WORLDLOG.md`

## Mandates Used

- Definition-driven systems only: event payloads remain structured data; HUD/log strings are generated by `world_log.ts`.
- Fixed-size history: event storage uses bounded ring buffers and save trimming.
- Parallel execution: no imports from agents 02-10; direct existing `state.msgs.push(...)` calls remain during partial migration.
- README describes shipped behavior only: README update deferred until final build passes.

## Baseline Map

- `src/core/types.ts`: add event/log state types plus optional `GameState.worldEvents`.
- `src/systems/events.ts`: new bounded event store API.
- `src/systems/world_log.ts`: new severity/privacy-aware consumer from event data to `Msg`/`LogEntry`.
- `src/main.ts`: initialize event store, tolerate old saves, trim save payload, publish kill events from existing `handleKill`, keep direct messages intact.
- `src/systems/samosbor.ts`: publish start/end/zone capture/fog boss lifecycle events.
- `src/systems/inventory.ts`: publish player pickup/drop/use/tool break/ammo consumption from centralized functions.
- `src/systems/quests.ts`: publish quest accepted/completed events after existing quest mutation.
- `src/systems/debug.ts`: expose recent important events and buffer counts in the existing debug overlay.
- `README.md`: add a factual bullet only after a successful build.

## Checklist

- [x] 1. Baseline map documented. DOD: integration points listed before edits. Rejected: touching unrelated modules. Estimate: +0 us runtime, saved future conflict scans.
- [x] 2. Event data types added. DOD: `WorldEventType`, `WorldEvent`, `WorldEventDraft`, `ContextFact`, `EventFilter`, capacities exist. Rejected: string-only log entries. Estimate: +0 us until events publish.
- [x] 3. `events.ts` ring buffer implemented. DOD: bounded recent/important/zone buffers, monotonic ids, query APIs, save trim. Rejected: unbounded arrays. Estimate: O(1) publish, <10 us/event.
- [x] 4. `world_log.ts` implemented. DOD: severity/privacy gates and soft dedupe by type+actor+target+zone. Rejected: every event becomes HUD spam. Estimate: <15 us/event, saves UI spam cost.
- [x] 5. `GameState.worldEvents` added and save/load tolerant. DOD: old saves missing field load. Rejected: migration hard fail. Estimate: +0 us load except normalization.
- [x] 6. Samosbor hooks added. DOD: start/warning/capture/end/fog boss spawn/kill events publish cheaply. Rejected: fog spread and fog minion per-cell/per-spawn event spam. Estimate: <50 us per samosbor transition.
- [x] 7. Inventory hooks added. DOD: player pickup/drop/use/tool break/ammo consume centralized through optional `GameState`. Rejected: projectile-level event calls. Estimate: <10 us per user action, ammo events severity 0.
- [x] 8. Quest hooks added. DOD: accepted/completed events publish after existing quest mutation/reward behavior. Rejected: reward path rewrite. Estimate: <10 us per quest transition.
- [x] 9. Kill hooks added. DOD: existing `handleKill` publishes player monster/NPC kills without changing XP/quest increments. Rejected: duplicate kill counters. Estimate: <10 us per kill.
- [x] 10. Debug visibility added. DOD: existing debug command list includes buffer counts and last 10 important events. Rejected: new UI mode or HUD signature change. Estimate: debug-only message cost.
- [x] 11. README updated if build passes. DOD: short factual implemented-system subsection added after passing checkpoint build. Rejected: design-plan prose. Estimate: +0 us runtime.
- [x] 12. Final build run and recorded. DOD: `npm run build` passed; no compile errors hidden. Rejected: skipping verification after README edit. Estimate: build-time only.

## Build Record

- Baseline `npm run build`: passed before event/log edits.
- Checkpoint after tasks 1-5 `npm run build`: passed.
- Checkpoint after tasks 6-10 `npm run build`: passed.
- Final `npm run build`: passed.

## Round 2

### Preflight Record

- XML prompt block extracted by id with `perl -0ne`.
- Read `README.md`, `architecture.md`, `audit.md`, `src/core/types.ts`, `src/systems/events.ts`, `src/systems/world_log.ts`, `src/systems/debug.ts`, `src/systems/production.ts`, `src/systems/containers.ts`, `src/systems/contracts.ts`, and `src/systems/rumor.ts`.
- Baseline before Round 2 edits: `npm run build` passed (`vite build`, 171 modules, `dist/index.html` 734.43 kB, gzip 227.10 kB).

### Event Audit

Published before Round 2:

- Samosbor lifecycle: warning, start, zone capture, end, fog boss spawn/kill.
- Player inventory: pickup, drop, use, tool break, ammo consumption.
- Quest lifecycle: created, completed.
- Player kills: monster and NPC kills.
- Production: output and input shortage, but not missing/full output containers.
- Containers: item take as `container_opened` or `item_stolen`.
- Contracts: creation was folded into `quest_created`.

Round 2 gaps:

- Production blockage needed structured reasons beyond input shortage.
- Contract creation/completion/failure needed contract-specific event types.
- Rumor spread/observation needed event types and at least one safe publisher.
- Important event health needed a grouped floor/zone debug view from bounded buffers.
- Event payloads needed stronger tag/data caps during publish and save normalization.
- Faction patrol clash and floor transition publishing remain deferred: the cheap publish sites are in AI/main call paths outside this pass's event/log hook scope.
- Direct event-to-rumor observation remains deferred: `publishEvent()` does not have an NPC witness list, and scanning entities from the event bus would violate the no full-world scan rule.

### Checklist

- [x] 1. Added Round 2 event types for production blockage, contract lifecycle, rumor observation/spread, faction clash, and floor transition. DOD: `WorldEventType` accepts the structured facts. Rejected: a second event bus. Estimate: +0 us until publish.
- [x] 2. Hardened event publish/save normalization. DOD: tags are capped/deduped, data is compacted to bounded primitive/simple records, old saved events are normalized. Rejected: storing arbitrary large payload objects. Estimate: <5 us/event for small records.
- [x] 3. Added production blockage telemetry. DOD: `no_container`, `no_inputs`, and `container_full` publish bounded production events; output events include zone ids. Rejected: per-frame production scans. Estimate: publish only on slow production ticks.
- [x] 4. Added contract-specific lifecycle events. DOD: contract creation publishes `contract_created`, completion publishes `contract_completed`, creation refusals publish `contract_failed`. Rejected: a separate contract runtime. Estimate: interaction/quest-completion only.
- [x] 5. Added rumor event publication. DOD: spoken static rumors publish quiet `rumor_spread`; `observeRumorEvent()` can publish `rumor_observed` when a future caller supplies state. Rejected: event bus scanning for witnesses. Estimate: talk-time only.
- [x] 6. Expanded world-log formatters and telemetry gates. DOD: player-understandable contract/theft/rumor/faction/floor strings exist; production/container-open/rumor-observed telemetry stays out of normal HUD spam. Rejected: logging every structured event. Estimate: no extra frame cost.
- [x] 7. Added debug floor/zone summary. DOD: `Последние события` groups important events by floor and zone by reading only the important ring buffer. Rejected: scanning `World` or all cells. Estimate: <=128 events when debug command is invoked.
- [x] 8. Updated README with shipped behavior only. DOD: event section mentions current producers and debug summary. Rejected: roadmap prose.
- [x] 9. Final `npm run build` and `npm run typecheck` recorded after docs/log polish. Result: both passed.

### Round 2 Verification

- Source checkpoint before docs: `npm run typecheck` passed after the AG01 source edits.
- Final `npm run typecheck`: passed.
- Final `npm run build`: passed (`vite build`, 201 modules, `dist/index.html` 999.77 kB, gzip 302.18 kB, built in 1.24s).
